// use strict mode
"use strict";

// Import required modules
import { checkSchema } from "./middlewares/schema";
import exact from "./middlewares/exact";
import oneOf from "./middlewares/one-of";
import { validationChainBuilders } from "./middlewares/validation-chain-builders";
import { MatchedData, ValidationResult } from "./matched-data";
import expressValidator from "./express-validator";

// Export all required functions and classes
export {
  exact,
  oneOf,
  validationChainBuilders,
  MatchedData,
  ValidationResult,
  expressValidator,
};

// Re-export checkSchema function
export { checkSchema };
