// error.ts

export const Error = class Error extends Error {
  constructor(message: string) {
    super(message);
    this.name = this.constructor.name;
  }
};



// index.js

const { Error } = require('./error');

try {
  throw new Error('Something went wrong');
} catch (err) {
  console.error(err.message);
}

