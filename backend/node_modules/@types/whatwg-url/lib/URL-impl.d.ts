import { Globals as WebIDLGlobals } from "webidl-conversions";
import { implementation as URLSearchParamsImpl } from "./URLSearchParams-impl";

export declare class URLImpl implements URL {
  constructor(globalObject: WebIDLGlobals, constructorArgs: [url: string, base?: string]);

  href: string;
  readonly origin: string;
  protocol: string;
  username: string;
  password: string;
  host: string;
  hostname: string;
  port: string;
  pathname: string;
  search: string;
  readonly searchParams: URLSearchParamsImpl;
  hash: string;

  toJSON(): string;

  // Added type annotations for the following methods
  toString(): string;
  //
