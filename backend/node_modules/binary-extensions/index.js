// Import the 'fs' module to read the JSON file
const fs = require('fs');

// Read the JSON file and parse it into a JavaScript object
const binaryExtensions = JSON.parse(fs.readFileSync('./binary-extensions.json', 'utf8'));

// Export the parsed object as the module
module.exports = binaryExtensions;

